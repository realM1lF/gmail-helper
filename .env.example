# Gmail Helper – Beispiel-Konfiguration
# Kopiere diese Datei nach .env und trage bei Bedarf Werte ein. .env wird nicht ins Repo eingecheckt.

# === Ollama (lokal) ===
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=mistral:7b-instruct

# === Gmail ===
GMAIL_Q=in:inbox is:unread newer_than:2d
MAX_RESULTS=20

# === Verhalten ===
DRY_RUN=false
SET_LABEL_COLORS=false
LOG_LEVEL=INFO

# Optionale, kommaseparierte Label-Liste (leer = Standardliste)
# LABELS_ALLOWED=Banking,Streaming,Rechnung,Warnung,Shopping,Social Media,Support,Newsletter,Versicherung,Sonstiges
