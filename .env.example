# Gmail Helper – Konfiguration
# ============================
# Kopiere diese Datei nach .env und passe bei Bedarf an.
# .env wird nicht ins Git-Repository eingecheckt (siehe .gitignore)


# === OLLAMA (Lokale KI) ===
# Basis-URL der Ollama-Instanz
# Standard: http://localhost:11434
OLLAMA_BASE_URL=http://localhost:11434

# Verwendetes KI-Modell
# Empfohlen: mistral:7b-instruct (~4.4 GB)
# Alternativen: llama3.1:8b, qwen2.5:7b-instruct, qwen2.5:3b (leichtgewichtig)
OLLAMA_MODEL=mistral:7b-instruct


# === GMAIL ===
# Gmail-Suchanfrage (Query) für zu klassifizierende E-Mails
# Siehe: https://support.google.com/mail/answer/7190
# Beispiele:
#   "in:inbox is:unread newer_than:2d"     = Ungelesene der letzten 2 Tage
#   "in:inbox newer_than:7d"                = Alle der letzten 7 Tage
#   "in:inbox label:Sonstiges newer_than:7d" = Re-Labeling für "Sonstiges"
GMAIL_Q=in:inbox is:unread newer_than:2d

# Maximale Anzahl E-Mails pro Lauf (1-100)
MAX_RESULTS=20


# === VERHALTEN ===
# Dry-Run Modus (nur anzeigen, keine Labels setzen)
# true = Testmodus, false = Live-Modus
DRY_RUN=false

# Label-Farben in Gmail setzen (experimentell)
# true = Farben zuweisen, false = Standardfarben
SET_LABEL_COLORS=false

# Logging-Level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO


# === ERWEITERT (Optional) ===
# Eigene Label-Liste (kommasepariert)
# Wenn leer oder auskommentiert: Standard-Liste wird verwendet
# Standard: Banking,Streaming,Rechnung,Warnung,Shopping,Social Media,Support,Newsletter,Versicherung,Sonstiges
# LABELS_ALLOWED=Banking,Streaming,Rechnung,Warnung,Shopping,Social Media,Support,Newsletter,Versicherung,Sonstiges
