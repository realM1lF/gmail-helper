# Gmail Helper – Beispiel-Konfiguration
# Kopiere diese Datei nach .env und trage echte Werte ein. .env wird nicht ins Repo eingecheckt.

# === OpenAI (wenn PROVIDER=openai) ===
OPENAI_API_KEY=
MODEL=gpt-4o-mini

# === Provider: openai | ollama ===
PROVIDER=openai

# === Ollama (wenn PROVIDER=ollama) ===
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:7b-instruct

# === Gmail ===
GMAIL_Q=in:inbox is:unread newer_than:2d
MAX_RESULTS=20

# === Verhalten ===
DRY_RUN=false
SET_LABEL_COLORS=false
LOG_LEVEL=INFO

# Optionale, kommaseparierte Label-Liste (leer = Standardliste aus config.py)
# LABELS_ALLOWED=Rechnungen,Support,Privat,Newsletter,Events,FYI,Banking,Versicherung,Angebote,Streaming,Gaming,Klamotten,Technik,Sport,Arbeit,Shopping,Account,Social Media,Sonstiges
